import React from 'react';

import {
    StyledContainer,
    StyledTitle,
} from './ShipList.styled';

const ShipList = () => {
    return (
        <StyledContainer>
            <StyledTitle>
                Select your perfect starships
            </StyledTitle>
        </StyledContainer>
    );
}

export default ShipList;
